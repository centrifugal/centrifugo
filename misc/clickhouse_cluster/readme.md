# Clickhouse Cluster

Clickhouse cluster with 2 shards and 2 replicas built with docker-compose.

## Run

```
make config
docker compose up
```

## Profiles

- `default` - no password
- `admin` - password `123`
